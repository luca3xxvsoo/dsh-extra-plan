# 按需规划模式实施后测试方案（草案）

> 状态：**草案 v0.1——测试原则与 A/B 方法已定；机制用例组为骨架**
> 来源：测试相关内容自实施阶段拆出

## 0. 范围与不变量

- 本方案只覆盖 extra-plan 预设及其新插件；
- **回归基线**：改 extra-plan 插件代码后，先跑场景测试 `test-extra-plan-gate.mjs`（112 用例）与预设静态校验 `validate-extra-plan-preset.mjs`（17 项，运行实测值）确认无回归，再跑本方案机制用例。

## 1. 测试原则（防坑）

1. **禁止复制判定逻辑**：插件必须导出 decisions 命名空间，场景测试直接 import 同一份实现（test-extra-plan-gate.mjs 模式）；测试中出现手写复制品 = 立即返工（v11.1 漂移教训）。
2. **工具名清单先校验**：开发首日写「清单存在性校验」脚本——deny/allow/planGated/alwaysGated 逐名对照宿主工具注册表；R2 教训（str_replace_editor 已更名 edit、bash 在 win32 未注册）不得重演。
3. **四通道核对表**：任何"裁剪/模型/deny"类机制，subagent / fork / workflow / ralph 四通道逐一取证（E8/堵泄漏教训），别默认通道间行为一致。
4. **取证优先**：断言看会话日志的 request/header 与 tool 事件（decode-session / print-header-tools 等取证工具可复用），不只看"看起来能跑"。
5. **生效前提核对**：预设改动 = 新会话生效（挂载世代）；插件改动 = 重启 web 进程；冒烟前先查 request/header 确认当前世代/代码版本。
6. **同任务对照口径**：A/B 用同一任务批、账本逐行取证；质量判定看最终产物逐字节/内容对比，不靠模型自评。

## 2. 机制用例组（骨架，开发后补全断言）

> 实施进度：**场景测试 `test-extra-plan-gate.mjs` 112/112 PASS**。**三轮冒烟完成，全部用例取证通过**：B1/B2（主/规划子代理 bootstrap 首 header 2 工具→晋升 22/12）、E1（力度继承 max→max）、G1-G6（三级闸门验词与流程实测：路由确认→澄清→规划→批准→执行→验收全链路走通）、P1（.extra-plan 双文件原子落盘，执行耗时 11~40ms）、L1（账本 main/planner/executor 三角色行）、Q2（断连恢复路径实测有效）、C1（探查硬上限护栏在场）。缺陷闭环：①pwsh 裸词 `md` 误伤 .md 文件名（v0.1.1 修复）；②批准问话加「修改意见」可空文本问题（复测生效）；③**save_plan 后子代理必死根因 = 插件自身 render 契约违规**（裸字符串 vs ContentBlock[]，v0.1.3 修复，RENDER1-3 契约测试锁死）——终轮复测规划子代理 save_plan 后正常续轮并 turn/end=completed，诊断文件零新错误。预设静态校验 17/17 PASS。④探查预算锚点改为自最近主会话消息起计、每条转达消息重置（v0.1.4，CU1-CU7 用例）；⑤规划任务附加指令 plannerPromptSuffix（v0.1.5，AP1-AP7 用例）；⑥save_probe 探查线索工具——新增 `validate-save-probe-gate.mjs`（v3：注册层/闸门五态/预算回归/真实落盘）与 gate 的 PR21 例 + RENDER4-10 渲染契约 7 例（并入 gate 总数 112）。

> 测试会话：新会话 + 手动选 flash 主会话 + 记录推理力度；每例先写"判据 → 取证位置 → 期望值"。

| ID | 机制 | 判据（草案） |
|---|---|---|
| B1 | 主会话 bootstrap | 首 request/header 工具 2 项（pwsh/read）+ 极简 persona；首个 tool/call 后恢复全量目录与 persona |
| B2 | 规划子代理 bootstrap | subagent_plan 子会话首 header 同款 2 项；model=pro；恢复后只读目录（无 write/edit） |
| G1 | 路由闸门 | 未完成路由 ask 前：write/edit 与 pwsh 写命令被拒、一切委派被拒；ask 完成后直行路径放行写工具 |
| G2 | 意图闸门 | 路由选规划但澄清 ask 未完成前：subagent_plan 被拒 |
| G3 | 计划闸门 | 计划批准 ask 未完成前：执行类委派（subagent/fork/workflow/ralph）被拒；向规划子代理 send_message 放行 |
| G4 | 问答续流豁免 | 规划子代理存续、计划未批准期间：用户消息不重置路由/澄清要求，send_message 直接放行 |
| G5 | 选项验词 | 路由 ask 三词解析：「直接执行」→直行态、「进行pro规划」→规划态、「不同意」→不动作转对话且闸门保持未确认；选项非固定枚举/空白回复（跳过本题）/取消/中断 → 不放行 + 提示重问；通道级错误码（NO_PROVIDER 等）→ 逃生放行；批准 ask 三词解析：「同意执行」放行执行、「转交pro规划」续轮、「不同意」读意见；空白/取消/三词不认 → 未批准不放行 |
| G6 | 直行态委派恒拒 | 路由选「直接执行」后：主会话 write/edit 放行，subagent/fork/workflow/ralph 仍被拒（无计划批准锚点） |
| Q1 | 疑问往返 | 规划子代理以疑问停轮 → 主会话收到 settlement 通知（子代理最终输出可见）→ 原样转达用户 → 用户答复 → send_message 续轮成功 |
| Q2 | 执行兜底 | 验收两轮不通过 → 主会话收集两轮不通过原因原样返回、由用户决策；无自动第三次重派；规划子代理批准后保留、用户拍板结束后释放 |
| E1 | 力度继承 | 主会话 header reasoningEffort=high → 规划子代理 request 同值；主 low → 子 low（完全继承无下限） |
| C1 | 探查硬上限 | 规划子代理自最近主会话消息起计的工具调用次数（排除 save_plan）超预算 → 后续探查调用被拒且收到"停止探查、收敛产出"指令；主会话新消息转达后预算重置 |
| P1 | 方案落盘 | save_plan 仅出现在规划子代理目录（主会话/执行者/reviewer 均无）；只写固定 `.extra-plan\` 目录、双文件 `方案-<任务名>-<时间戳>.md` + `验收-<任务名>-<时间戳>.md`（含路径分隔符/超长任务名被净化）、传入任意路径被拒；**只传一个 payload 被拒**；写入后两文件必然同时存在且内容一致；执行者读两文件、reviewer 只读验收文件 |
| O1 | 越界回传 | 执行子代理遇工作区外写入需求 → 写入汇报的越界清单（命令/路径/用途/预期内容）、不自行尝试；主会话普通 pwsh 拿拒绝标记 → sandbox_permissions 重试 → 审批弹窗（仅 workspace-write + ask 档） |
| L1 | 账本 | 新预设会话行写入 ledger.jsonl；(sessionId,seq) 去重；重启不重记；行 ts 为折叠时刻 |
| R1 | 防递归 | 规划/执行子代理工具目录无委派类工具、无 subagent_review；规划子代理无写工具 + pwsh 写动词被拦 |
| C2 | 直行执行 | 路由确认"直接执行"后主会话 write/edit 可用、完工自查汇报；未确认前不可用（与 G1 互证） |
| SP1 | save_probe 注册层 | 只出现在主会话工具目录（规划子代理/执行者/reviewer 均无）；重复注册幂等（取证：validate-save-probe-gate.mjs S1-S5） |
| SP2 | save_probe 硬闸门 | 无路由→拒 / 选规划未澄清→拒 / 选规划已澄清→放行 / 直行态→拒 / 通道错误逃生→放行；拒因含「探查线索未放行」（取证：validate-save-probe-gate.mjs S6-S10） |
| SP3 | 线索文件内容契约 | 落盘 `.extra-plan` 下**单个** `线索-<任务名>-<时间戳>.md`，含「# 探查线索（save_probe 落盘，非结论）」卷首声明 + 四节标题（一、文件地图/二、重点区域/三、排除项/四、背景与意图），只写定位线索不含证据摘录，返回文件路径（取证：test-extra-plan-gate.mjs RENDER4-10 + 真机冒烟） |
| SP4 | 校验上限 | 条目 20/20/10/10、总量 ≤4096 字符、fileMap/focusAreas 的 path 不存在即拒、range 格式 `L12-34` 合法 `L12-x` 拒（取证：test-extra-plan-gate.mjs PR1-PR21） |

## 3. 成本 A/B 对照（上线后）

- **口径**：同任务批（沿用 8 项批量同任务 A/B 口径）；对照组 = 直行路径（主会话直接执行、零 pro）；实验组 = 规划路径（澄清 + pro 规划 + 批准后执行）；判据 = pro 成本是否被方案质量与执行效率抵消（简单任务应直行）。
- **指标**：pro:flash 花费比、总花费、质量判定（产物逐字节/内容对比）、无效调用（被闸门拒绝次数、往返轮次）。
- **预期管理（如实）**：单轮小复杂任务可能打平（规划子代理冷启动是固定成本）；收益面 = 简单任务零 pro + 大任务/多轮迭代（continuable 缓解冷启动）。
- **结论写回**：本文档 §3（附实测数据），用于校准「简单任务直行 vs 复杂任务规划」的路由判断。

## 4. 遗留待办

- 以《README.md》机制描述为基线，按需补全 §2 每条的具体断言（判据/取证位置/期望值）；
- 快通道两条教训（不引入启动预锁、收益面匹配负载）作为路由类用例的设计约束沿用。
