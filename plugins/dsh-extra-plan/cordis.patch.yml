# 预设分发已迁移至 postinstall（scripts/distribute-preset.mjs，安装时一次性执行）：
# 本 bundle patch 注册 flash-guide（flash 近场引导）：flash-guide 不再单独上架，
# 随主包安装并由主包代挂；用户可在 设置 → 插件 → dsh-extra-plan-settings 中开关。
- insert:
    - id: flash-guide
      name: '@local/dsh-extra-plan/flash-guide'
